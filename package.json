{
  "name": "modwav-web-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only smoke tests for the Mod.wav static site. NOT shipped — index.html has zero runtime dependencies.",
  "scripts": {
    "test": "playwright test",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
